# Local / OS
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/

# Secrets — never commit
*.json
!package.json
.env
credentials*

# Large local Excel dumps (optional; keep runs/ CSVs if small)
# *.xlsx

# Google Drive Doc stubs (real Doc lives in Drive)
*.gdoc

# Experiment BQ caches (local only)
experiments/cache/
*.parquet

# Google Sheets Drive stubs
*.gsheet
