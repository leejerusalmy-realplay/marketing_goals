# SQL steps (Excel checks)

One file per calculation. Naming: `NN_short_name.sql` (e.g. `01_population_assignment_rp.sql`).

Each file should:

- Be runnable alone in BigQuery
- Return a small result Lee can export to Excel
- State at the top what it proves

Empty until step 1 is written.
