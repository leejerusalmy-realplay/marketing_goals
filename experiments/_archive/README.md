*Lee Jerusalmy*

# experiments/_archive

Loose copies parked here so `experiments/` root stays clean.

- `Marketing_Goals_Combined_RP_LS_Colab.ipynb` — old loose copy (generic lives in `notebooks/`; variants under `cv_optimization/<name>/`).

Safe to delete later with Lee permission.

